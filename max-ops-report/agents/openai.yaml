interface:
  display_name: "MAX OPS Reporter"
  short_description: "一条命令安全连接任务并回报 MAX OPS"
  default_prompt: "Use $max-ops-report to connect this explicitly authorized task to its MAX OPS URL, retain one run ID, and report the Agent run with questions, artifacts, acknowledgements, and receipts."
